require 'rails_helper'

describe OldExam do
  it "cannot have empty date"
  it "cannot have an invalid date"
  it "cannot have empty title"
  it "cannot have empty examiners"
  it "searches for exact date"
  it "searches for partly match title"
  it "searches for partly match examinars"
end