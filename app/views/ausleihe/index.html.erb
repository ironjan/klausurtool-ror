<%= page_heading 'Ausleih-Tool der FSMI' %>

<div class="clearfix spacer">

  <%= form_tag ausleihe_switch_path, :method => 'post', id: 'form', html: {class: 'pure-form', role: 'form'} do %>

      <div class="form-group row">
        <%= label_tag :folderList, 'Ordner:', :class => 'control-label' %><br>
        <%= text_area_tag :folderList, params[:folderList], placeholder: 'Ordnerliste', required: true,
                          rows: 6, columns: 10, :class => 'form-control', autofocus: true  %>
      </div>

      <div class="form-group row">
        <%= submit_tag '(W)eiter', class: 'pure-button pure-button-primary', :accesskey => 'w' %>
        <%= submit_tag 'Zu(r)ücksetzen', :id => 'reset_button', :type => 'reset', class: 'pure-button', :accesskey => 'r' %>
      </div>
  <% end %>


</div>
